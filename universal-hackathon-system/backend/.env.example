# Backend environment variables
SECRET_KEY=dev_secret_change_me
ACCESS_TOKEN_EXPIRE_MINUTES=60
CORS_ORIGINS=http://localhost:5173

DATABASE_URL=postgresql+psycopg2://uhs:uhs@localhost:5432/uhs
REDIS_URL=redis://localhost:6379/0

SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=demo@example.com

# Scheduler cron (example every minute)
SCHEDULER_CRON=*/1 * * * *